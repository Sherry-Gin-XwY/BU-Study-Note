<div>
    <div class="table-responsive">
        <table class="table table-condensed">
            <thead>
                <tr>
                    <td>
                        <button class="btn btn-primary" onclick="viewChart('{{customerId}}')">View Carts</button>
                    </td>
                                    </tr>
                                    <tr>
                    <td>
                        <button class="btn btn-danger" onClick="logout();">Logout</button>
                    </td>
                </tr>
            </thead>
            {{!-- TODO: just save for testing --}}
            <p>Customer id: <Strong>{{customerId}}</Strong></p>
            {{#if data}}
            <thread>
                <tr style=font-size:30px>
                    <th>Shop Id</th>
                    <th>Shop Name</th>
                    <th>Amount</th>
                    <th>Price</th>
                    <th>Operation</th>
                </tr>
            </thread>
            {{else}}
            <tr>
                <td colspan="3">No Shop right now!</td>
            </tr>
            {{/if}}
            <tbody>
                <input type="hidden" name="customerId" value="{{customerId}}">
                {{#each data}}
                <tr>
                    <td>{{this.id}}</td>
                    <td>{{this.shopName}}</td>
                    <td>{{this.amount}}</td>
                    <td>{{this.price}}</td>

                    {{!-- hide the value need to pass--}}

                    <input type="hidden" name="shopId" value="{{this.id}}">
                    <input type="Hidden" name="shopName" value="{{this.shopName}}">
                    <td>
                        <a href="/customerShopPage/addToChart/{{this.customerId}}/{{this.id}}">Add To Cart</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>